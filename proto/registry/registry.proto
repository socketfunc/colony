syntax = "proto3";

package colony.registry;

message DownloadRequest {
  string file = 1;
}

message DownloadResponse {
  bytes data = 1;
}

service Registry {
  rpc Download(DownloadRequest) returns (stream DownloadResponse) {}
}
